// ignore_for_file: file_names

import 'package:flutter/material.dart';

class UserVideoCall extends StatelessWidget {
  const UserVideoCall({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('VideoCall'));
  }
}
